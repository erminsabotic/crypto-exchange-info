import { green, red } from "@mui/material/colors";

const buyTableRowStyles = {
  backgroundColor: green[200],
};

const sellTableRowStyles = {
  backgroundColor: red[200],
};

export { buyTableRowStyles, sellTableRowStyles };
